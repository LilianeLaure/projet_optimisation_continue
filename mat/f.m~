function Y=f(X,C)
%X et C sont de meme dimension 
    Y=0;
    [m,n]=size(C);
    Cnew=[C(1,:)];
    Xnew=[X(1,:)];
    for i=2:m
        Cnew=[Cnew,C(i,:)];
        Cnew=[Xnew,
    end
    
        for j=1:n
            Y=Y+C(i,j)*X(i,j);
        end
    end
end
